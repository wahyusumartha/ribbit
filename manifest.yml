---
applications:
  .:
    name: ribbit
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: ap-aws
    url: ${name}.${target-base}
    mem: 256M
    instances: 1
